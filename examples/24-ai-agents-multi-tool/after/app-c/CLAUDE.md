# app-c — Claude Code guidelines

Rust 1.92.

> This file is generated by Weaver. Shared conventions live in `AGENTS.md`.
> Edits here will be overwritten. Put hand-written guidance there.

## Conventions

- Prefer `?` over `.unwrap()` outside of tests.
- New dependencies require justification in the commit message.

## Claude-specific

- Prefer `Read`, `Grep`, and `Glob` over ad-hoc `Bash` commands for file exploration.
- Use the `.claude/skills/` directory for reusable, named workflows.
